#include <iostream>
#include <string>

using namespace std;

#define endl '\n'

int main()
{
    // char enter = '\n';       // (newline) перевод каретки на следующую строку
    // char tab = '\t';         // (tab) табуляция (отступ, красная строка)
    // char space = ' ';    
    // char return = '\r';      // (return) перевод каретки на в начало текущей строки
    // char backspace = '\b';   // (backspace) перевод каретки на один символ назад

    // int n;
    // cin >> n;
    // for (int i = 0; i < n; i++) 
    // {
    //     int a;
    //     cin >> a;
    //     cout << a << '\n';
    // }
    
    // int n;
    // cin >> n;
    // for (int i = 0; i < n; i++) 
    // {
    //     string str;
    //     cin >> str;
    //     cout << str << '\n';
    // }

    // int n;
    // string str;
    // cin >> n;
    // getline(cin, str);
    // cout << str << '\n';
    // for (int i = 0; i < n; i++) 
    // {
    //     getline(cin, str);
    //     cout << str << '\n';
    // }

    return 0;
}